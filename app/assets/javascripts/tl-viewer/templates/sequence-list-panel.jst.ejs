<h4>Narratives for this Leaf</h4>
<ul>
	<% _.each( sequences, function( sequence ) { %>
		<li><a href='#' class='sequence-link' data-sequence-id='<%= sequence.id %>'><%= sequence.name %></a> by username.</li>
	<% }); %>
</ul>
<div id='sequence-panel'></div>